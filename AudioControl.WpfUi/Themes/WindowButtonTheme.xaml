<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:controls="clr-namespace:AudioControl.WpfUi.Core.Controls">
    <Style x:Key="WindowButtonTheme"
           TargetType="{x:Type controls:RoundedButton}">
        <Setter Property="Foreground"
                Value="{StaticResource Color2}" />
        <Setter Property="Background"
                Value="{StaticResource Color0}" />
        <Style.Triggers>
            <Trigger Property="IsMouseOver"
                     Value="True">
                <Setter Property="Foreground"
                        Value="{StaticResource Color4}" />
                <Setter Property="Background"
                        Value="{StaticResource Color1}" />
            </Trigger>
            <Trigger Property="Button.IsPressed"
                     Value="True">
                <Setter Property="Foreground"
                        Value="{StaticResource Color2}"></Setter>
                <Setter Property="Background"
                        Value="{StaticResource Color4}" />
            </Trigger>
        </Style.Triggers>
    </Style>
</ResourceDictionary>