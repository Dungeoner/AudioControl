<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="AudioControl.UI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AudioControl.UI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:models="using:AudioControl.UI.Models"
    mc:Ignorable="d">

    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
        <ListView 
            ItemsSource="{x:Bind Devices}"
            Width="Auto">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:Device">
                    <Grid>
                        <local:DeviceControl Name="DeviceControl" Device="{x:Bind}"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackPanel>
</Window>
